---
spring:
  profile: production
heroku:
  access-token: ${HEROKU_ACCESS_TOKEN}
  webhook:
    secret: ${HEROKU_WEBHOOK_SECRET}
    events-url: https://api.heroku.com/apps/vangos-secret-keeper/webhook-events
bitly:
  api-url: https://api-ssl.bitly.com/v4/bitlinks
  access-token: ${BITLY_ACCESS_TOKEN}
---
spring:
  profile: development
heroku:
  access-token: not+applicable
  webhook:
    secret: not+applicable
    events-url: https://api.heroku.com/apps/vangos-secret-keeper/webhook-events
bitly:
  api-url: https://api-ssl.bitly.com/v4/bitlinks
  access-token: not+applicable
